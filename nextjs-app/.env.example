# Database Configuration
# For development (SQLite) - uses local file-based database
DATABASE_URL="file:./energy.db"

# For production (PostgreSQL) - configure with your database credentials
# DATABASE_URL="postgresql://user:password@localhost:5432/energydb?schema=public"

# Switch provider in prisma/schema.prisma:
# - Development: provider = "sqlite"
# - Production: provider = "postgresql"
